## Übung Optimierung: Aufgabe 3
import numpy as np
import matplotlib.pyplot as plt
from typing import Tuple

def f(x: float, y: float) -> float:
    val = None
    return val

def f_grad(x: float, y: float) -> Tuple[float, float]:
    x_grad = None
    y_grad = None
    return x_grad, y_grad

def optimize(x_start: float, y_start: float) -> Tuple[float, float]:
    x_opt = None
    y_opt = None
    return x_opt, y_opt
